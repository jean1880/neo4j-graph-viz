graph.json
.playwright-mcp/
__pycache__/
/target
frontend/node_modules/
frontend/dist/

# Screenshots and other captured images are debugging artifacts, never source. The app ships
# no raster assets, so a blanket ignore is safe — force-add with `git add -f` if that changes.
*.png
*.jpg
*.jpeg
*.gif
*.webp

# Local AI-assistant context. AGENTS.md is the committed, shareable version; CLAUDE.md and
# .claude/ hold machine- and deployment-specific notes that must not be published.
CLAUDE.md
.claude/
.cursor/
.aider*

# Scratch diffs from refactors — never an artifact worth tracking.
*.patch

# OS / editor cruft and local env files
.DS_Store
._*
Thumbs.db
desktop.ini
*.swp
*.swo
*~
.idea/
.env
.env.*
!.env.example
!.env.sample

# Runtime logs
*.log

# Rust build artifacts
/target/
**/*.rs.bk
*.pdb

# Node build artifacts and deps
node_modules/
dist/
.vite/
coverage/
npm-debug.log*
yarn-error.log*
.npm/
